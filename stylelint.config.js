/**
 * Stylelint configuration for Tailwind CSS project
 * Extends standard rules, Tailwind-specific allowances, and Prettier compatibility.
 */
module.exports = {
  // Tailwind CSS specific Stylelint config (handles @tailwind at-rules)
  extends: ['stylelint-config-tailwindcss'],
  rules: {},
};
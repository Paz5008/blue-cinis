---
trigger: always_on
---

Animation (Framer Motion) : "Utiliser `m` au lieu de `motion` avec `LazyMotion` pour le tree-shaking. Les animations doivent utiliser les features importées via `domAnimation`. Importer depuis `framer-motion` le composant `m` et non `motion`."
